void setup() {
  Serial.begin(9600);  // Initialize the primary hardware serial port at 9600 baud
}

void loop() {
  if (Serial1.available()) {
    char receivedChar = Serial1.read();
    Serial.println("Received: " + String(receivedChar));
  }
}
